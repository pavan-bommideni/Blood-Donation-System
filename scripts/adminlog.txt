CREATE TABLE  "ADMINLOG" 
   (	"AID" NUMBER NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(40) NOT NULL ENABLE, 
	"DONORID" NUMBER NOT NULL ENABLE, 
	"DOCTORID" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "ADMINLOG_PK" PRIMARY KEY ("AID") ENABLE
   )
/


			
	
