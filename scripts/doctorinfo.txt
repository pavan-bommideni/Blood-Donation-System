CREATE TABLE  "DOCTORINFO" 
   (	"DID" NUMBER NOT NULL ENABLE, 
	"NAME" VARCHAR2(40) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(400) NOT NULL ENABLE, 
	"HOSPITAL_DETAILS" VARCHAR2(400) NOT NULL ENABLE, 
	"PHONE_NUMBER" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "DOCTORINFO_PK" PRIMARY KEY ("DID") ENABLE
   )
/


			
	
