CREATE TABLE  "DONORREG" 
   (	"ID" NUMBER NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(400) NOT NULL ENABLE, 
	"NAME" CHAR(100) NOT NULL ENABLE, 
	"PHONE_NO" NUMBER NOT NULL ENABLE, 
	"BLOOD_GROUP" VARCHAR2(20) NOT NULL ENABLE, 
	"AVALIABLE_DAYS" VARCHAR2(4000) NOT NULL ENABLE, 
	"ADDRESS" VARCHAR2(4000) NOT NULL ENABLE, 
	"AGE" NUMBER NOT NULL ENABLE, 
	"GENDER" VARCHAR2(40) NOT NULL ENABLE, 
	 CONSTRAINT "DONORREG_PK" PRIMARY KEY ("ID") ENABLE
   )
/