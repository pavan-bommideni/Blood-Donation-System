CREATE TABLE  "PATIENT" 
   (	"PID" NUMBER NOT NULL ENABLE, 
	"NAME" VARCHAR2(40) NOT NULL ENABLE, 
	"AGE" NUMBER NOT NULL ENABLE, 
	"BLOOD_GROUP" VARCHAR2(40) NOT NULL ENABLE, 
	"MEDICAL_DETAILS" VARCHAR2(4000) NOT NULL ENABLE, 
	"GENDER" VARCHAR2(40) NOT NULL ENABLE, 
	 CONSTRAINT "PATIENT_PK" PRIMARY KEY ("PID") ENABLE
   )
/
